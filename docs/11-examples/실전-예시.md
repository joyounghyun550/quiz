# 실전 예시

📍 **위치**: [홈](../00-README.md) > [예시](../11-examples/) > 실전 예시

---

## 1. 게시글 기능 구현 (posts)

### 폴더 구조

```
src/
├── app/
│   └── posts/
│       ├── page.tsx                        # 게시글 목록
│       └── [id]/
│           └── page.tsx                    # 게시글 상세
│
├── features/
│   └── post/
│       ├── api/
│       │   ├── post.api.ts
│       │   ├── post.api.type.ts
│       │   └── index.ts
│       ├── hooks/
│       │   ├── query/
│       │   │   └── index.ts
│       │   └── index.ts
│       ├── components/
│       │   └── index.ts
│       ├── utils/
│       │   ├── post-params.util.ts
│       │   └── index.ts
│       ├── types/
│       │   ├── pagination.type.ts
│       │   ├── pagination-number.type.ts
│       │   └── index.ts
│       └── index.ts
```

### 페이지 컴포넌트

```tsx
// app/posts/page.tsx
import { PostList } from "@/features/post/components";

export default function PostsPage() {
  return (
    <div className="container mx-auto py-8">
      <h1 className="mb-6 text-3xl font-bold">게시글 목록</h1>
      <PostList />
    </div>
  );
}
```

```tsx
// app/posts/[id]/page.tsx
import { PostDetail } from "@/features/post/components";

type Props = {
  params: { id: string };
};

export default function PostDetailPage({ params }: Props) {
  return <PostDetail postId={params.id} />;
}
```

---

## 2. 채팅 기능 구현 (chat)

### 폴더 구조

```
src/
├── app/
│   └── chat/
│       └── page.tsx                        # 채팅 페이지
│
├── features/
│   └── chat/
│       ├── api/
│       │   ├── chat.api.ts
│       │   ├── chat.api.type.ts
│       │   └── index.ts
│       ├── hooks/
│       │   ├── query/
│       │   │   └── index.ts
│       │   ├── mutation/
│       │   │   ├── use-delete-chat-session-mutation.ts
│       │   │   └── index.ts
│       │   └── index.ts
│       ├── components/
│       │   └── index.ts
│       ├── constants/
│       │   ├── chat-page.constant.ts
│       │   └── index.ts
│       ├── utils/
│       │   ├── message-formatter.util.ts
│       │   ├── chat-history.util.ts
│       │   └── index.ts
│       ├── types/
│       │   └── index.ts
│       └── index.ts
```

### 페이지 컴포넌트

```tsx
// app/chat/page.tsx
import { ChatContainer } from "@/features/chat/components";

export default function ChatPage() {
  return <ChatContainer />;
}
```

---

## 3. 인증 기능 구현 (auth)

### 폴더 구조

```
src/
├── app/
│   └── (auth)/
│       ├── login/
│       │   └── page.tsx                    # 로그인 페이지
│       └── auth/
│           └── callback/
│               └── page.tsx                # OAuth 콜백
│
├── features/
│   └── auth/
│       ├── api/
│       │   ├── auth.api.ts
│       │   ├── auth.api.type.ts
│       │   └── index.ts
│       ├── hooks/
│       │   ├── query/
│       │   │   ├── use-current-user-profile-query.ts
│       │   │   └── index.ts
│       │   ├── mutation/
│       │   │   ├── use-save-terms-agreement-mutation.ts
│       │   │   ├── use-withdraw-mutation.ts
│       │   │   └── index.ts
│       │   └── index.ts
│       ├── components/
│       │   ├── LoginForm.tsx
│       │   ├── AuthCallbackHandler.tsx
│       │   ├── TermsAgreementDialog.tsx
│       │   └── index.ts
│       └── index.ts
```

### 페이지 컴포넌트

```tsx
// app/(auth)/login/page.tsx
import { LoginForm } from "@/features/auth/components";

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <LoginForm />
    </div>
  );
}
```

---

## 4. Hybrid 페이지 예시

서버에서 초기 데이터를 페칭하고, 클라이언트에서 인터랙션을 처리하는 패턴:

```tsx
// app/posts/page.tsx - Server Component
import { PostList } from "@/features/post/components";
import { PostSearchBar } from "@/features/post/components/PostSearchBar"; // Client Component

/**
 * 게시글 목록 페이지
 * 서버에서 초기 데이터를 페칭하고, 클라이언트에서 인터랙션 처리합니다.
 */
export default async function PostsPage() {
  // ✅ 서버에서 초기 데이터 페칭
  const initialPosts = await fetch("https://api.example.com/posts").then((res) => res.json());

  return (
    <div>
      <h1>게시글 목록</h1>

      {/* ✅ 검색 인터랙션 - Client Component */}
      <PostSearchBar />

      {/* ✅ 목록 표시 - Server Component 데이터를 Client Component에 전달 */}
      <PostList initialData={initialPosts} />
    </div>
  );
}
```

---

## 5. 동적 라우트 예시

```tsx
// app/community/[id]/page.tsx
import type { Metadata } from "next";

type Props = {
  params: { id: string };
  searchParams: { [key: string]: string | string[] | undefined };
};

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const post = await getPostById(params.id);

  return {
    title: post.title,
    description: post.excerpt,
  };
}

export default function PostDetailPage({ params, searchParams }: Props) {
  return <div>게시글 ID: {params.id}</div>;
}
```

---

**관련 문서**:

- [폴더 구조](../02-architecture/폴더-구조.md)
- [컴포넌트 구조](../06-code-style/컴포넌트-구조.md)
- [Server vs Client Component](../07-nextjs-specific/Server-vs-Client-Component.md)

**최종 업데이트**: 2026년 2월 4일
